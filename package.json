{
  "name": "webhook-catcher",
  "version": "1.0.0",
  "description": "A minimal webhook.site-style request catcher. Every request to /in/:binId is logged and viewable in a live dashboard.",
  "main": "server.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
